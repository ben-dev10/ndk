export const siteConfig = {
  name: "ndk-ui",
  url: "https://ndk-ui.vercel.app/",
  description:
    "Nard Development Kit (NDK) - a UI library of components and frontend toolkit for developers",
};

export type SiteConfig = typeof siteConfig;
